		<script src="https://code.jquery.com/jquery-3.4.1.min.js" crossorigin="anonymous"></script>
		<script src="https://cdn.jsdelivr.net/npm/typed.js@2.0.9"></script>
		<script>
  		$(document).ready(function() {
    			var description = new Typed('#typed', {
        		strings: ['Giving made easy', 'Change starts here', 'Donate for good', 'Empower the world', 'Join the movement'],
        		loop : "true",
        		cursorChar:"",
        		backDelay:"2000",
        		typeSpeed:"9.8",
        		backSpeed:"7"

    		})
		})
		</script>
<div class="home-container w-100">
<div class="bg-shadow d-flex align-items-center justify-content-center w-100">
    <div class="text-center d-block w-100" >
<h1 class="text-white heading"><span id="typed"></span>.</h1>


<%# Search bar %>
  <%= form_tag charities_path, method: :get, class: 'text-center search-form d-flex px-5 search-bar' do %>
    <%= select_tag :cause, options_for_select([["All", ""], *Cause.all.map{ |c| [c.name, c.id] }]), prompt: "Select a cause", class: "form-select me-3 py-4" %>
    <%= submit_tag "Search", class: "btn btn-success" %>
  <% end %>
  </div>
</div>
<%# Moving text %>


</div>
